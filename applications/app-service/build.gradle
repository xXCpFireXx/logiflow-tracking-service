apply plugin: 'org.springframework.boot'

apply plugin: 'io.spring.dependency-management'

dependencies {
	implementation project(':rest-consumer')
	implementation 'org.springframework.boot:spring-boot-starter-webflux'
	implementation 'org.reactivecommons.utils:object-mapper:0.1.0'
	implementation project(':mongo-repository')
	implementation project(':reactive-web')
    implementation project(':model')
    implementation project(':usecase')
    implementation 'org.springframework.boot:spring-boot-starter'

    // Agregamos el cliente de config
    implementation 'org.springframework.cloud:spring-cloud-starter-config'

    // Agregamos bootstrap para que el "idioma" sea compatible
    implementation 'org.springframework.cloud:spring-cloud-starter-bootstrap'

    runtimeOnly('org.springframework.boot:spring-boot-devtools')
    testImplementation 'com.tngtech.archunit:archunit:1.4.1'
    testImplementation 'tools.jackson.core:jackson-databind'
}

    // elige las versiones correctas
dependencyManagement {
    imports {
        // Usamos la misma version que el servidor para que sean "hermanos"
        mavenBom "org.springframework.cloud:spring-cloud-dependencies:2025.1.1"
    }
}

tasks.register('explodedJar', Copy) {
    with jar
    into layout.buildDirectory.dir("exploded")
}

jar {
    // To disable the *-plain.jar
    enabled = false
}

bootJar {
    // Sets output jar name
    archiveFileName = "${project.getParent().getName()}.${archiveExtension.get()}"
}